# requirements.lock — pinned, reproducible runtime install for the DynaMix package.
# Covers core deps + the 'milp' extra (pulp). Excludes dev/build tools and the editable
# package itself. Regenerate with: pip-compile / pip freeze after changing pyproject.toml.
# Resolved for Python >=3.12 (these latest pins — e.g. numpy 2.5 — require >=3.12). The core
# package still supports 3.11 via the loose floors in pyproject.toml: on 3.11 use
# `pip install -e .` (pip resolves 3.11-compatible versions) rather than this lock.
#
joblib==1.5.3
narwhals==2.22.1
numpy==2.5.0
packaging==26.2
pandas==3.0.4
plotly==6.8.0
PuLP==3.3.2
python-dateutil==2.9.0.post0
scikit-learn==1.9.0
scipy==1.18.0
six==1.17.0
threadpoolctl==3.6.0
